import { cn } from "@/lib/utils";

interface Props {
    className?: string;
}


export const Auth: React.FC<Props> = ({ className }) => {

    
    return (
        <>
            <section className={cn('px-2 sm:px-3 lg:px-4', className)}>
                fgew
            </section>
        </>
    )
}
